#include <stdio.h>
#include <stdlib.h>
#include "binary-sort.h"
#include "array_list.h"

int main(int argc, char *argv[])
{
    puts("Main File Running");
    // main_binary_sort(argc, argv);
    main_al(argc, argv);
    return 0;
}