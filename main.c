#include <stdio.h>
#include <stdlib.h>
#include "binary-search.h"
#include "array_list.h"

int main(int argc, char *argv[])
{
    puts("Main File Running");
    main_binary_search(argc, argv);
    // main_al(argc, argv);
    return 0;
}